import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oncloudhealth.app',
  appName: 'OnCloudHealth',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
